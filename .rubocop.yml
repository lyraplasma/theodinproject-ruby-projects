inherit_from:
  - .rubocop_todo.yml
  - ~/.rubocop.yml

plugins:
  - rubocop-performance
AllCops:
  NewCops: enable

Style/StringLiterals:
  EnforcedStyle: double_quotes

Style/FrozenStringLiteralComment:
  EnforcedStyle: never

inherit_gem:
  prettier: rubocop.yml